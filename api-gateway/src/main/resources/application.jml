server:
  port: 8080  # API Gateway runs on port 8080

spring:
  application:
    name: api-gateway

grpc:
  client:
    lpr-service:
      address: "static://lpr-service:8081"
      negotiationType: plaintext

  ssl:
    enabled: false 
